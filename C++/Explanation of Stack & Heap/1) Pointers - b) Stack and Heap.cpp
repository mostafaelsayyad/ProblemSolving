
NO CODE HERE =P

Memory is devided into parts:
- Stack
- Heap
»⁄÷ ›Êﬁ Ê‘ÊÌ…Õ«Ã«  =D


Stack:
when the program starts, for every function called including the main
a frame is reserved in the stack, any variable declared inside the function
is reserved inside its frame until this function end and returns
to where the function was called from, the frame is released from
the memory

stack size are limited
functions with all their variables are reserved in it

Heap:
large pool of memory, the developer is responsible for requesting
a memory from it, and is responsible for releasing this memory
when he don't need it

